import FullPages from '@/components/caregivers/FullPages';
import React from 'react';

const page = () => {
    return (
        <div>
            <FullPages/>
        </div>
    );
};

export default page;